using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class logger : MonoBehaviour
{
    public Camera cam;

    void Start()
    {
        
    }

    
    void Update()
    {
        //print("my dist with cam " + Vector3.Distance(transform.position, cam.gameObject.transform.position));
    }
}
